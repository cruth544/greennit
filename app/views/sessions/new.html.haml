%h1 Log in

- unless current_user
  %div.my-forms
    = render "partials/sessions/log_in"
- else
  %div
    You are logged in as #{current_user.username}
  %div
    =link_to("Log out?", destroy_session_path)
