%div
  %span.pull-left
    %span
      = link_to("Home", root_path)
      = "|"
      = link_to("All", posts_all_path)
      = "|"
    - if current_user
      %span Subgreens:
      - if current_user.subgreens.length > 0
        - current_user.subgreens.each do |sub|
          %span
            = link_to(sub.name, subgreen_path(sub))
            - if current_user.subgreens.last != sub
              = "|"
      - else
        = link_to("Browse", subgreens_path)
  %span.pull-right
    - if current_user
      %span
        / - binding.pry
        = image_tag(current_user.profile_pic, width: "25px", height: "25px")
      %span
        = link_to(current_user.username, user_path(current_user))
    - else
      = link_to("Log in", new_session_path)
%br
%br
/ .user-image
/   = image_tag @user.profile_image_uri, width: "300px", class: "img-circle"
/   .caption.text-over-image
/     %h1= @user.handle
/     - if params[:action] == "show"
/       %p= link_to "Create a mumble", new_mumble_path(@user), class: "btn btn-primary btn-lg", role: "button"
/     - else
/       %p= link_to "Edit your profile", edit_user_path(@user), class: "btn btn-primary btn-lg", role: "button"
/ %h3
/   %em= @mumblr_message || "Welcome to Mumblr…"
