$('.post-index').append('<%= j render "partials/posts/post_index" %>');
<% if @posts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @posts %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove()
<% end %>
